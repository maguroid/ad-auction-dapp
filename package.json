{
  "name": "@auction-dapp/monorepo",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "start": "yarn workspace @auction-dapp/react start",
    "chain": "yarn workspace @auction-dapp/truffle chain",
    "compile": "yarn workspace @auction-dapp/truffle compile",
    "console": "yarn workspace @auction-dapp/truffle console",
    "debug": "yarn workspace @auction-dapp/truffle debug",
    "develop": "yarn workspace @auction-dapp/truffle develop",
    "gh-deploy": "yarn workspace @auction-dapp/react deploy",
    "migrate": "yarn workspace @auction-dapp/truffle migrate",
    "postmigrate": "yarn gen-types",
    "ctr-test": "yarn workspace @auction-dapp/truffle test",
    "gen-types": "yarn workspace @auction-dapp/react gen-types"
  }
}
